# Enumerable#cycle on a custom includer (counted / endless-with-break /
# empty receiver), alongside native array and range receivers -- pins the
# coverage a prelude fragment must NOT shadow (see packages/prelude).
